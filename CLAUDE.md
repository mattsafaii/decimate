# Decimate

Native Mac companion app for Affinity Designer/Photo. Affinity has no programmatic image effects and can't run Photoshop plugins, so Decimate covers that gap: algorithmic effects applied to an image and exported back into Affinity. v1 is personal tooling; long-term vision is a full-featured distributed app with a growing effect catalog.

## Current cycle: v2 — Affinity bridge + refocused catalog

**Building now.** v2 turns Decimate into the native compute engine bolted onto Affinity V3's new MCP/JavaScript scripting bridge. Decimate-driven round-trip: Pull the active Affinity document full-res → apply a unique effect → Send the result back as a new layer (editable curves for vector effects, raster for dithering). The catalog refocuses on what Affinity structurally can't do; the v1 effects that just rebuilt Affinity-native filters are removed. Big batch (1–2 weeks), second cycle on this repo.

The spike (verified June 7 2026) drove Affinity end-to-end via the local MCP at `localhost:6767`: read the active document, wrote + read a Desktop file from inside an Affinity script. Round-trip primitives confirmed — `doc.export` (full-res out), Bitmap/PixelBuffer + `addNode` (raster in), `CurveBuilder`/`PolyCurve` (editable vector in). No native plugin needed.

**The bridge (Decimate-driven):** MCP is the control channel, a Desktop temp file is the bulk-data channel. Pull = `execute_script` → `doc.export` full-res to a Desktop temp file → load into preview. Send = render full-res → write Desktop temp file → `execute_script` to inject as a new layer (`addNode(ImageNodeDefinition)` for raster, `CurveBuilder`/`PolyCurve` for vector). Mandatory read-preamble handshake before `execute_script`. Detects unreachable MCP / `NOT_ALLOWED` / `PERMISSION_DENIED` and guides setup (Affinity's "Allow scripts to access filesystem" + AI connector enabled). Graceful "Affinity not connected" state; standalone open→effect→export still works.

**v2 surfaces:**
- Main window (existing v1 shell) extended with an Affinity connection-status indicator and Pull / Send actions
- Affinity bridge controls: Pull from Affinity, Send to Affinity (raster layer or editable curves per the effect's output type) + setup guidance
- Palette editor (for dithering): bundled palette or in-session custom
- First-launch venv setup (existing)

**v2 effect catalog:**

| Effect | Engine | Output |
|---|---|---|
| Vector Stipple (anchor) | Python/SciPy → Swift curves | editable vector curves |
| Vector Engraving / Hatching | tone-following lines | editable vector curves |
| Vector Line-screen | halftone as vector lines | editable vector curves |
| Dithering (palette + OKLab, multiple kernels, color-correct) | Swift | raster |

Dithering absorbs v1's Floyd-Steinberg/Bayer. Removed from v1: raster Halftone, Noise, raster Voronoi (Affinity-native).

**v2 key types:**
- `EffectDeclaration` (existing) — gains color/palette/toggle parameter types and an `outputType: raster | vector`
- `Palette` — name + ordered colors; bundled presets + in-session custom
- `AffinityBridge` — MCP client: connect, readPreamble, execute, pull, sendRaster, sendVector; status (connected / no-Affinity / permission-denied)

**v2 no-gos (in addition to the standing list below):**
- Rebuilding Affinity-native effects (halftone, raster Voronoi, glitch, add-noise, pixelate, posterize, threshold, gradient map)
- Affinity-driven / Scripts-panel-first trigger UX (Decimate-driven this cycle)
- Native Affinity plugin (not possible on macOS)
- New third-party Swift dependencies (build the MCP client natively)
- Saved/managed user palette library beyond bundled presets + in-session custom
- Networking-channel integration and live/bidirectional sync (one-shot pull→process→push only)

The v1 architecture below still holds — read it for the engine rules, Python bridge contract, stippling point-list contract, and preview pipeline.

## Stack

- Swift + SwiftUI, Xcode project, app name **Decimate**, bundle id `com.safaii.decimate`
- Targets current macOS, Apple Silicon
- Project is generated by xcodegen from `project.yml` — after adding/removing files, run `xcodegen generate` (never hand-edit the pbxproj)
- CoreImage for built-in filters
- Python 3 + SciPy via subprocess for numerical algorithms (no PythonKit, no sockets)
- **No third-party Swift dependencies in v1**

## Architecture

**Effect catalog as abstraction** — each effect is a declaration: name, engine, parameter list (label, type, range, default), supported output formats. The shell renders parameter UI and routes execution from the declaration. New effects must be additive — no shell surgery.

**Engine decision rule:**
- CoreImage if the filter exists built-in
- Pure Swift for sequential per-pixel algorithms (dithering)
- Python subprocess for numerical/vector algorithms (stippling)
- Custom Metal shader if an effect is per-pixel parallel but not in CoreImage's catalog (no v1 effect needs Metal — future option only)

**Python bridge** — file-based CLI contract: Swift writes input image + params JSON to a temp dir, invokes the script via `Process` against the app-managed venv, reads the output file back.

**Python setup** — app creates its own venv under `~/Library/Application Support/` from bundled `python/requirements.txt` on first launch, with visible progress. Blocks Python-engine effects (not the app) until complete. Clear error if no `python3` found. Skipped on later launches.

**Stippling contract** — the Python script returns the **point list (positions + sizes)** as its result, not a finished image. Swift renders points to SVG/PNG. This keeps future custom-shape stippling a rendering-layer change only.

**Preview pipeline** — preview renders on a downsampled copy (full-res Voronoi can take minutes); export renders full resolution.

## Surfaces

- **Main window:** image preview, effect picker, parameter panel (rendered from declarations), export action
- **First-launch setup:** venv creation with progress
- **Export panel:** save dialog offering the formats the selected effect declares (PNG; PNG + SVG for stippling)

## Core flow

Open image (picker or drag-drop) → pick effect → adjust params (downsampled live preview) → export full-res (PNG, or SVG for stippling) → SVG imports into Affinity as editable vector dots.

## v1 effect catalog

| Effect | Engine | Output |
|---|---|---|
| Stippling (weighted Voronoi) | Python/SciPy | SVG + PNG |
| Floyd-Steinberg dithering | Swift | PNG |
| Bayer dithering | Swift | PNG |
| Halftone | CoreImage | PNG |
| Noise | CoreImage | PNG |

## No-gos (do not build these)

- Distribution packaging (signing, notarization, bundled Python runtime, installer)
- Video processing
- Batch processing of multiple images
- Custom icon/shape stippling modes (architecture leaves the door open; not built in v1)
- Affinity plugin integration — standalone companion, files move via export/import
- Effect stacking/chaining — one effect at a time
- No features beyond the Build todolist without asking Matt first

## Basecamp

Repo config is set — `basecamp` commands work without flags from this directory.

- Account: `REDACTED` (Safaii Studio)
- Project: `REDACTED` (Decimate)
- Build todolist (v2 — current): `REDACTED`
- PRD doc (v2): https://app.basecamp.com/REDACTED/buckets/REDACTED/documents/REDACTED
- Pitch + PRD card (v2): https://app.basecamp.com/REDACTED/buckets/REDACTED/card_tables/cards/REDACTED
- v1 Build todolist (done): `REDACTED` · v1 PRD doc: https://app.basecamp.com/REDACTED/buckets/REDACTED/documents/REDACTED

Workflow: work the Build todolist in order, comment the commit SHA on each todo before checking it off.

## Solo
This project runs inside Solo, which manages its processes. For coordination and follow-up work, call `help(topic="coordination")` and `help(topic="timers")`.
